import Button from "../Inputs/Button";
import "./styles.scss";

const Header = () => {
  return (
    <header className="container card-2 header">
      <div>Yummy</div>

      <Button>Favorites</Button>
    </header>
  );
};

export default Header;
